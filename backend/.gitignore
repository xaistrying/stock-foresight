.idea
.ipynb_checkpoints
.mypy_cache
.vscode
__pycache__
.pytest_cache
htmlcov
dist
site
site_zensical_src
.coverage*
coverage.xml
.netlify
test.db
log.txt
Pipfile.lock
env3.*
env
docs_build
site_build
.venv
docs.zip
archive.zip

# vim temporary files
*~
.*.sw?
.cache

# macOS
.DS_Store

.codspeed

# SQLite data — generated by the app (see design.md Migration Plan:
# "created fresh by this change"), not source. Reproducible via
# POST /tickers/{ticker}/load — do not commit.
data/*.db

# Trained model artifacts — generated by the training pipeline, not source.
# Reproducible by re-running training against app.db — do not commit.
data/models/*
!data/models/.gitkeep